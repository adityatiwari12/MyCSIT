name: mycsit
description: MyCSIT - Student Intelligence Platform for CSIT Department, AITR
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase - older version to avoid web plugins
  supabase_flutter: ^1.5.0

  # State Management
  flutter_riverpod: ^2.3.6

  # UI
  cupertino_icons: ^1.0.5

  # Utilities
  uuid: ^3.0.7
  go_router: ^12.1.3
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

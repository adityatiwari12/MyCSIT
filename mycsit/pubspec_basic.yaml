name: mycsit
description: MyCSIT - Student Intelligence Platform for CSIT Department, AITR
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase - basic version
  supabase_flutter: ^1.2.0

  # State Management
  flutter_riverpod: ^2.3.6

  # UI
  cupertino_icons: ^1.0.5

  # Utilities - no web dependencies
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
